/**
 * @Purpose this is used to create a product object in typescript
 * @author Coolname
 */
export interface Product{
    id: string;
    color: string;
    weight: number;
    type: string;
    price: number;
    quantity: number;
}